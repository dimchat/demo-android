apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation group: 'chat.dim', name: 'SDK', version: '0.5.16'
    implementation group: 'chat.dim', name: 'Plugins', version: '0.3.2'

    implementation group: 'chat.dim', name: 'DOS', version: '0.1.2'
    implementation group: 'chat.dim', name: 'DMTP', version: '0.1.0'
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
